.Tweet-show
  .Tweet-show-article
    .Tweet-show-title
      = @tweet.title
    .Tweet-show-image
      = image_tag @tweet.image.url,class: "Tweet-show-img"
    %br
    .Tweet-show-text
      = @tweet.text
    - if user_signed_in? && current_user.id == @tweet.user_id
      .more
        %ul.more_list
          %li
            = link_to '編集', edit_tweet_path(@tweet.id), method: :get
          %li
            = link_to '削除', tweet_path(@tweet.id), method: :delete
          %li
            = link_to 'カレンダーに投稿する', new_blog_path(id: @tweet.id)

    -# %span.name
    -#   %a{:href => "/users/#{@tweet.user.id}"}
    -#     %span 投稿者
    -#     = @tweet.user.nickname

    -# - if @tweet.image?
    -#   = image_tag @tweet.image.url
    -# - @tweets.each do |tweet|
    -#   = link_to tweet_path(tweet.id) ,class: "details" do
    -#     = image_tag @tweet.image.url
    -#     = tweet.title
    -#     = tweet.text
    -#     = tweet.user.nickname

  .container
  - if current_user
    = form_with(model: [@tweet, @comment], local: true, id: "new_comment") do |form|
      = form.text_area :text, placeholder: "コメントする" , rows: "2", class: "textbox"
      = form.submit "SEND", class: "form__submit"
  - else
    %strong
      %p ※※※ コメントの投稿には新規登録/ログインが必要です ※※※
  .comments
  %h4 ＜コメント一覧＞
  - if @comments
    - @comments.each do |comment|
      %p
        %strong
          = link_to comment.user.nickname, "/users/#{comment.user_id}"
          ：
        = comment.text
