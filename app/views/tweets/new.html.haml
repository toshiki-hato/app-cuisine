.wrapper
  = render "title_bar"
\\-# .Post
\\-#   .Post__form
\\-#     = form_with model: @tweet, html: {class: "Post__form__input"}, local: true do |f|
\\-#       .Input
\\-#         .Input__title
\\-#           = f.text_field :title, class: 'Input__title',placeholder: '献立のタイトル'
\\-#         .Input__article
\\-#           = f.file_field :image, class: 'file-box'
\\-#           = f.text_field :text, class: 'Input__text',placeholder: 'type a message'
\\-#           -# = f.label :image, class: 'Input__image' do
\\-#           -#   -# = icon('far','image', class: 'icon')
\\-#         = f.submit 'Send', class:'Input__btn'
.contents.row
  .container
    = form_with(model: @tweet, local: true) do |form|
      %h3 投稿する
      = form.text_field :title, placeholder: "title"
      = form.label :image
      = form.file_field :image
      = form.hidden_field :image_cache # 後述
      = form.text_area :text, placeholder: "text", rows: "10"
      = form.submit "SEND"
